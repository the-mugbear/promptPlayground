const loader = document.getElementById('loader');
const characters = '01アイウエオカキクケコサシスセソタチツテトナニヌネノハヒフヘホマミムメモヤユヨラリルレロワヲン';
const columnCount = 10;

for (let i = 0; i < columnCount; i++) {
  const column = document.createElement('div');
  column.className = 'column';
  column.style.left = (i * 10) + 'px';
  column.style.animationName = 'fall';
  column.style.animationDelay = (Math.random() * 2) + 's';
  
  const characterCount = Math.floor(Math.random() * 10) + 5;
  
  for (let j = 0; j < characterCount; j++) {
    const char = document.createElement('div');
    char.className = 'character';
    char.textContent = characters.charAt(Math.floor(Math.random() * characters.length));
    column.appendChild(char);
  }
  
  loader.appendChild(column);
}

// Example usage
{/* <div class="matrix-loader" id="loader">
<!-- Columns and characters will be generated by JavaScript -->
</div> */}