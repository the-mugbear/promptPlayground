When testing invalid chars we send the string "Hello <char of interest>" and I believe some chars aren't exited safely within the python causing a client error.

Test Run
    Change disposition to an AJAX call so it doesn't reload page each time the user selects a disposition

